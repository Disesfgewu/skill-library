# Metadata

Name: amdahls_law
Category: computer_architecture/performance
Tags: [amdahls-law, speedup, parallelism, diminishing-returns]
Dependencies: [speedup]
Related Skills: [parallel/multithreading]
Difficulty: advanced

---

# Purpose

`speedup` 說明如何量測「整個系統」變快了多少倍，但實務上最佳化幾乎從不是「整個系統」一起變快——通常只有系統的**一部分**被加速（例如只有某段運算被平行化、只有某個硬體元件被升級）。Amdahl's Law 回答一個非常實際、也非常容易被忽略的問題：只加速一部分，對整體的幫助**上限**是多少？

---

# Concepts

Amdahl's Law 公式：
```
Overall Speedup = 1 / [ (1 - Fraction_enhanced) + (Fraction_enhanced / Speedup_enhanced) ]
```
- **Fraction_enhanced**：被加速的那部分，佔原本總執行時間的比例（0 到 1 之間）。
- **Speedup_enhanced**：那部分本身被加速了幾倍。
- 未被加速的部分（`1 - Fraction_enhanced`）維持原本的執行時間，完全不受影響。

---

# Theory

## 核心洞察：未被加速的部分決定了整體加速比的天花板

即使 `Fraction_enhanced` 那部分被加速到**無限快**（`Speedup_enhanced → ∞`，也就是那部分的執行時間趨近於 0），公式化簡為：
```
Overall Speedup → 1 / (1 - Fraction_enhanced)
```
這代表整體加速比存在一個**硬性上限**，完全由「有多少比例沒被加速」決定，跟被加速的那部分加速了多少倍**無關**。例如只加速 30% 的部分（`Fraction_enhanced = 0.3`），不管那 30% 加速幾倍，整體加速比永遠不可能超過 `1 / (1 - 0.3) = 1/0.7 ≈ 1.43` 倍——這是 Amdahl's Law 最重要、也最常被誤解的結論：**收益遞減（diminishing returns）不是「可能發生」，而是這個公式的數學必然結果。**

## 對硬體設計與平行化的實務啟示

這解釋了為什麼「增加更多處理器核心」不會無限提升效能：如果一個程式有 20% 的部分本質上是序列（sequential，無法平行化，例如需要等前一步結果才能算下一步），不管加多少核心平行化剩下 80%，整體加速比永遠不會超過 `1/0.2 = 5` 倍。這也是為什麼在做效能最佳化時，Jserv 式的「量化勝過直覺」特別重要：直覺上「加速某個部分」聽起來總是好事,但如果沒有先算出那部分佔的比例，可能投入大量工程資源加速一個只佔整體 5% 執行時間的環節，即使那部分加速到無限快，整體效能提升也不到 5.3%（`1/0.95 ≈ 1.053`）。

---

# Hardware

（不適用 — Amdahl's Law 是純粹的數學/量化分析工具，適用於任何「部分加速、部分不變」的系統，硬體或軟體皆可）

---

# Software

計算範例：一個程式 30% 的執行時間花在某段運算,這段運算被平行化加速 2 倍：
```
Fraction_enhanced = 0.3
Speedup_enhanced = 2

Overall Speedup = 1 / [ (1 - 0.3) + (0.3 / 2) ]
                = 1 / [ 0.7 + 0.15 ]
                = 1 / 0.85
                ≈ 1.176
```
即使那 30% 的部分加速了 2 倍，整體也只加速了約 1.18 倍——因為佔 70% 的部分完全沒變。

---

# Example

比較「加速比例較小的部分很多倍」跟「加速比例較大的部分較少倍」哪個整體效果更好：
```
情境 A：加速 10% 的部分，加速 100 倍
Overall Speedup = 1 / [ 0.9 + (0.1/100) ] = 1 / [0.9 + 0.001] = 1 / 0.901 ≈ 1.110

情境 B：加速 50% 的部分，加速 2 倍
Overall Speedup = 1 / [ 0.5 + (0.5/2) ] = 1 / [0.5 + 0.25] = 1 / 0.75 ≈ 1.333
```
情境 A 把一小部分加速了整整 100 倍，整體卻只提升約 11%；情境 B 只把一半的部分加速 2 倍（遠不如 100 倍誇張），整體反而提升約 33%——這具體證明了「加速的比例（Fraction_enhanced）」往往比「加速的倍率（Speedup_enhanced）」對整體效能影響更大，是效能工程中決定優化優先順序的關鍵依據。

---

# Common Mistakes

- 如果你認為「把某個環節加速越多倍，整體效能一定提升越多」，上方 Example 的具體數字已經反駁了這個直覺：加速 100 倍的情境 A，整體提升遠不如只加速 2 倍但涵蓋範圍更大的情境 B。正確的最佳化優先順序判斷，永遠要先問「這個環節佔整體執行時間的比例是多少」，而不是先問「這個環節能加速幾倍」。
- 忽略 Amdahl's Law 假設「未被加速的部分完全不受影響」這個前提——如果加速某部分的同時，反而讓其他部分變慢（例如平行化引入額外的同步開銷，見 `parallel/synchronization`），實際整體效能可能比公式算出的理論值更差，公式給出的是理論上限，不是保證能達到的實際值。

---

# Interview Questions

- 為什麼 Amdahl's Law 說明了「增加處理器核心數」不會無限提升效能？
- 給定兩個最佳化方案，一個加速比例小但倍率高、一個加速比例大但倍率低，怎麼用 Amdahl's Law 判斷該優先做哪個？

延伸思考：如果一個系統的序列部分（無法平行化的部分）本身也能透過其他方式縮短（不是平行化，而是演算法改進），這對 Amdahl's Law 算出的加速比上限會有什麼影響？這是否代表「先想辦法縮小序列部分」比「增加更多平行化資源」更值得優先投入？

---

# Summary

Amdahl's Law：`Overall Speedup = 1 / [(1-F) + F/S]`，其中 F 是被加速部分佔的比例、S 是那部分加速的倍率；未被加速的部分（1-F）決定整體加速比的天花板，即使 F 那部分加速到無限快，整體加速比也不會超過 `1/(1-F)`；效能最佳化的優先順序應該優先考慮「佔比多大」而非「能加速幾倍」，這是量化分析而非直覺能得出的結論。
