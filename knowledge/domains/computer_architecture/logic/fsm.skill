# Metadata

Name: fsm
Category: computer_architecture/logic
Tags: [fsm, finite-state-machine, moore, mealy, control-unit]
Dependencies: [synchronous_digital_systems, combinational_logic]
Related Skills: []
Difficulty: intermediate

---

# Purpose

`synchronous_digital_systems` 說明了如何用時脈讓系統「記得」一個值，但沒有規定「記得的值該怎麼隨時間變化」。有限狀態機（Finite State Machine, FSM）是把「系統目前處於哪個狀態」與「什麼條件下轉移到哪個狀態」形式化的抽象模型，是設計任何有「行為順序」的數位電路（最典型的例子：CPU 的 Control Unit）的標準方法。

---

# Concepts

一個 FSM 由以下要素組成：

- **State（狀態）**：系統在某個時間點所處的情況，用暫存器（`synchronous_digital_systems`）儲存目前狀態的編碼。
- **Transition（轉移）**：在什麼輸入條件下，從一個狀態換到另一個狀態，是組合邏輯（`combinational_logic`）依「目前狀態 + 目前輸入」算出「下一個狀態」。
- **Output（輸出）**：FSM 依狀態（與/或輸入）產生的輸出訊號。

依輸出的產生方式分兩種：

- **Moore 機**：輸出只取決於**目前狀態**，不直接看輸入。
- **Mealy 機**：輸出取決於**目前狀態與目前輸入**。

---

# Theory

## Moore 機 vs. Mealy 機的實務差異

Moore 機的輸出在一個時脈週期內是穩定的（因為只跟狀態有關，狀態在這個週期內不變），但通常需要更多狀態才能表達同樣的行為（因為輸出必須綁定在狀態本身）；Mealy 機的輸出能對輸入變化立即反應（少一個週期的延遲），狀態數通常較少，但輸出可能在同一個週期內因輸入變化而變化（less "clean" 的時序）。CPU 的 Control Unit 兩種設計都存在，選擇取決於是否可以接受輸出隨輸入立即變化。

## 為什麼 FSM 是 Control Unit 設計的標準方法

CPU 執行指令的過程（尤其是多週期/microcode 風格的設計）本質上就是「依目前處於哪個執行階段 + 目前解碼出的指令，決定這個週期要送出哪些控制訊號、以及下一個週期該進到哪個階段」——這正好完全對應 FSM 的定義：狀態 = 目前執行到哪個階段，轉移條件 = 指令類型/解碼結果，輸出 = 控制訊號。單週期或 pipeline 化設計則是把 FSM 簡化/展開成更直接的組合邏輯（見 `datapath/control_unit`），但概念根源仍然是 FSM。

---

# Hardware

FSM 的一般硬體結構：

```
                 +------------------+
   Input ------->|                  |
                 |  Next-State      |
                 |  Logic (組合邏輯)  |----> Next State
   Current   +-->|                  |
   State     |   +------------------+
             |
             |   +------------------+
             +-->|  State Register  |
                 |  (同步元件)        |
                 +--------+---------+
                          |
                          v
                    Current State ---> Output Logic (組合邏輯) ---> Output
```

`State Register` 是同步元件（在時脈邊緣更新，見 `synchronous_digital_systems`），`Next-State Logic` 與 `Output Logic` 都是純組合邏輯。

---

# Software

FSM 在 Verilog/Chisel 中常見的兩段式（或三段式）寫法：一段用 `always @(posedge clk)` 描述狀態暫存器如何更新（同步部分），另一段用組合邏輯描述 next-state 與 output 邏輯——這個寫法結構直接對應上方 Hardware 段的方塊圖，是數位邏輯課程中最常見的 FSM coding style。

---

# Example

一個簡單的「偵測連續兩個 1」的 Moore 機：狀態有 `S0`（尚未看到 1）、`S1`（剛看到一個 1）、`S2`（連續看到兩個 1，輸出 1）。輸入 0 時從 `S1`/`S2` 都回到 `S0`；輸入 1 時 `S0→S1`、`S1→S2`、`S2` 維持 `S2`。輸出邏輯（Moore）：只有狀態為 `S2` 時輸出 1，其餘狀態輸出 0——輸出只看狀態，不直接看當下輸入，這正是 Moore 機的定義。

---

# Common Mistakes

- 把 Moore 機的輸出誤寫成同時依賴輸入（變成事實上的 Mealy 機），卻仍稱之為 Moore 機。
- 忽略 Next-State Logic 與 Output Logic 都是純組合邏輯，設計時卻不小心引入了記憶性（例如用組合邏輯區塊卻寫出了 latch），導致行為不可預期。

---

# Interview Questions

- Moore 機跟 Mealy 機在輸出時序上的差異是什麼？各自適合什麼情境？
- 為什麼 CPU 的 Control Unit 設計常被建模成一個 FSM？

---

# Summary

FSM 用「狀態 + 轉移條件 + 輸出」形式化系統的行為順序，State Register 是同步元件、Next-State/Output Logic 是純組合邏輯；Moore 機輸出只依狀態、Mealy 機輸出同時依輸入；CPU 的 Control Unit 設計是 FSM 最典型的應用場景。
