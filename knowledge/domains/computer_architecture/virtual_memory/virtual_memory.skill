# Metadata

Name: virtual_memory
Category: computer_architecture/virtual_memory
Tags: [virtual-memory, address-translation, protection, os]
Dependencies: []
Related Skills: [page_table, tlb, cache/memory_hierarchy]
Difficulty: intermediate

---

# Purpose

`foundations/c_memory_layout` 描述一個程式看到的記憶體佈局（Text/Data/Heap/Stack），但沒解釋一個關鍵問題：如果多個程式同時執行，它們各自的「記憶體佈局」怎麼可能都從同一個位址開始，卻不會互相干擾？虛擬記憶體（Virtual Memory）正是解決這個問題（以及更多）的機制：每個程式看到的位址空間是**虛構的**，由硬體與作業系統合作，動態轉換成實際的實體記憶體位址。

---

# Concepts

虛擬記憶體同時解決三個問題：

1. **位址空間獨立（Isolation）**：每個程式（process）都以為自己獨佔一整個位址空間（例如 32-bit 系統上的 0 到 2^32-1），不需要知道其他程式在實體記憶體的哪裡，也無法直接存取其他程式的記憶體。
2. **記憶體保護（Protection）**：一個程式不能存取未被授權的位址，防止程式（不論惡意或有 bug）破壞其他程式或作業系統本身的記憶體。
3. **記憶體超額配置（Overcommit / Demand Paging）**：所有程式要求的虛擬記憶體總和可以超過實際實體記憶體大小，作業系統依實際使用情況動態決定哪些資料真正留在實體記憶體、哪些暫存到硬碟。

實現方式：把虛擬位址空間切成固定大小的**分頁（page）**，每個分頁可以獨立對應到（或尚未對應到）一個實體記憶體中的**頁框（page frame）**，這個對應關係記錄在 `page_table`。

---

# Theory

## 虛擬記憶體跟 `memory_hierarchy` 的本質關聯

虛擬記憶體常被獨立當成「作業系統的功能」介紹，但從硬體/效能的角度看,它其實是 `cache/memory_hierarchy` 描述的記憶體階層的**再往下延伸一層**：主記憶體（DRAM）相對於硬碟/SSD，也符合「小而快、大而慢」的關係，虛擬記憶體的 demand paging 機制，本質上就是把主記憶體當成硬碟的「cache」——分頁不在主記憶體中（page fault）時，才去硬碟把它載入,跟 cache miss 時去下一層記憶體要資料是同一套邏輯，只是這裡的「miss penalty」大到以毫秒計（硬碟存取），而不是 cache miss 的奈秒等級。

## 為什麼一定要有硬體支援（不能純軟體）

理論上「把虛擬位址轉成實體位址」可以完全用軟體查表完成，但這代表**每一次**記憶體存取都要先執行一段查表程式碼——這會讓每個 `lw`/`sw` 指令的成本暴增好幾倍。因此位址轉換的**查表本身**（沿著 `page_table` 這個資料結構走訪）雖然邏輯上可以由軟體定義,但硬體必須提供直接支援才能達到可接受的效能，這正是 `tlb`（一塊專門加速這個查表過程的硬體）存在的直接理由。

---

# Hardware

虛擬位址到實體位址的轉換概念示意：
```
虛擬位址: | Virtual Page Number | Page Offset |
                    |
                    v  (查 page_table)
實體位址: | Physical Frame Number | Page Offset |
```
`Page Offset`（分頁內的位移量）在轉換前後不變——只有「這個分頁對應到哪個實體頁框」需要透過 `page_table` 查詢轉換,分頁大小（例如 4KB）決定 Offset 要用幾個 bit。

---

# Software

作業系統維護每個 process 各自獨立的 page table，process 切換（context switch）時，作業系統會切換到對應這個新 process 的 page table，這也是為什麼不同的 process 即使使用相同的虛擬位址（例如都認為自己的程式從虛擬位址 `0x10000` 開始），實際存取到的實體記憶體完全不同、互不干擾。

---

# Example

Process A 跟 Process B 都各自認為自己有一個從虛擬位址 `0x1000` 開始的變數：A 的 page table 把 `0x1000` 對應到實體頁框 `0x5000`，B 的 page table 把同樣的虛擬位址 `0x1000` 對應到完全不同的實體頁框 `0x9000`。兩個 process 用同樣的虛擬位址存取，硬體依「目前正在執行哪個 process」查對應的 page table，轉換出不同的實體位址，讀寫到各自獨立的實體記憶體，不會互相看到對方的資料。

---

# Common Mistakes

- 如果你認為「虛擬記憶體只是為了讓記憶體看起來變大（超額配置）」，這個直覺只捕捉到三個目的中的一個（Overcommit），忽略了 Isolation 與 Protection 同樣是虛擬記憶體存在的核心理由——即使一台機器的實體記憶體大到完全不需要超額配置，虛擬記憶體仍然必要，因為沒有它就無法讓多個 process 安全地互不干擾。可以想像：如果沒有虛擬記憶體，任何一個 process 只要寫錯一個指標，就能直接覆寫到作業系統或其他 process 的記憶體。
- 把虛擬記憶體的位址轉換想成「額外、可有可無的效能負擔」，而忽略如果沒有硬體加速（見 `tlb`），這個負擔會大到讓系統完全無法接受——正確理解是「硬體支援讓這個必要機制的代價降到可接受範圍」，不是「這個機制本身沒有代價」。

---

# Interview Questions

- 虛擬記憶體同時解決哪三個問題？為什麼即使實體記憶體很充足，虛擬記憶體仍然必要？
- 虛擬記憶體的 demand paging 機制，跟 `cache/memory_hierarchy` 描述的記憶體階層邏輯有什麼共通之處？

延伸思考：如果 page size 設計得非常小（例如 64 bytes），對 page table 的大小、對 TLB 的命中率會分別造成什麼影響？

---

# Summary

虛擬記憶體讓每個 process 擁有獨立、受保護的虛構位址空間，同時支援記憶體超額配置；透過把位址空間切成 page、用 page_table 記錄虛擬到實體的對應關係實現；本質上是記憶體階層再往硬碟延伸一層（demand paging 邏輯與 cache miss 相同），因為查表成本過高，硬體支援（見 tlb）是達到可接受效能的必要條件。
