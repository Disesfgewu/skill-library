# Metadata

Name: multithreading
Category: computer_architecture/parallel
Tags: [multithreading, race-condition, parallelism, concurrency]
Dependencies: []
Related Skills: [synchronization, cache_coherency, performance/amdahls_law]
Difficulty: intermediate

---

# Purpose

`performance/amdahls_law` 談到平行化能提升效能，但平行執行多個執行緒（thread）會引入一個單執行緒程式完全不存在的新問題類別：多個執行緒同時存取共用資料時,結果可能取決於「誰先誰後」這個不可預測的執行時序——這就是 race condition，是理解 `synchronization` 存在必要性的起點。

---

# Concepts

- **Thread（執行緒）**：一個程式內可以獨立排程執行的控制流,多個 thread 可以共用同一個 process 的記憶體位址空間（相對於 process 之間，見 `virtual_memory` 的位址空間隔離）。
- **Race Condition（競爭條件）**：多個執行緒同時存取共用資料，且至少一個是寫入操作，結果依賴於執行時序，可能在不同次執行得到不同（甚至錯誤）的結果。
- **Critical Section（臨界區）**：程式中存取共用資料、必須確保同一時間只有一個執行緒能進入的那段程式碼。

---

# Theory

## Race Condition 的根源：看似單一的操作其實不是原子的

最經典的例子：`counter++`（遞增一個共用變數）看起來是一個操作，但編譯成硬體指令後其實是三個步驟：`讀取 counter 到暫存器` → `暫存器加 1` → `把暫存器寫回 counter`。如果兩個執行緒幾乎同時執行這三個步驟,可能發生：兩者都先讀到 counter=5，各自加 1 得到 6，各自寫回 6——明明遞增了兩次，counter 卻只從 5 變成 6，而不是預期的 7。這正是「操作看似單一，實際上由多個非原子步驟組成」，是幾乎所有 race condition 的根本原因。

## 為什麼 Race Condition 特別難除錯

Race condition 的發生與否，取決於兩個執行緒的指令交錯順序——這個順序受作業系統排程、硬體時序等許多因素影響，**不保證每次執行都會發生**。程式可能在測試環境跑一千次都正常，部署到生產環境後才在某個罕見的時序下出錯。這正是 `shared/jserv_style.skill` 強調「量化勝過直覺」在這個主題格外重要的原因：「這段程式碼看起來沒問題、測試也都過了」不能作為「沒有 race condition」的證據，必須從程式邏輯本身分析是否存在未受保護的共用資料存取，而不能只靠測試結果的直覺判斷。

---

# Hardware

（不適用 — race condition 是軟體層次的邏輯問題；硬體層次的類似問題見 `cache_coherency`）

---

# Software

```c
// 共用變數
int counter = 0;

// 兩個 thread 都執行這段程式碼
void increment() {
    counter++;   // 非原子操作：讀取 -> 加1 -> 寫回，三個步驟
}
```
若兩個 thread 同時呼叫 `increment()`，counter 最終值**可能**是 2（正確），也**可能**是 1（race condition 導致其中一次遞增遺失）——結果不確定，這正是 race condition 的具體表現。

---

# Example

用時序圖具體呈現上方 counter 的 race condition：
```
Time:      T1                          T2
t0:        讀取 counter (=5)
t1:                                    讀取 counter (=5)
t2:        counter = 5 + 1 = 6
t3:        寫回 counter (=6)
t4:                                    counter = 5 + 1 = 6  <- 用的是 t1 讀到的舊值
t5:                                    寫回 counter (=6)     <- 覆蓋掉 T1 的結果

最終 counter = 6（應該是 7，遺失了一次遞增）
```

---

# Common Mistakes

- 如果你認為「`counter++` 這種看起來很簡單的操作應該不會有 race condition 問題」，這個直覺把「程式碼看起來是一行」跟「這一行對應的硬體操作是原子的」搞混了——上方 Example 已經用具體時序圖證明,`counter++` 實際上是三個獨立步驟，可以透過反組譯這行程式碼、觀察它確實展開成多條硬體指令來驗證這一點。
- 認為「測試跑很多次都沒問題，所以這段程式碼沒有 race condition」——race condition 的發生依賴不保證重現的執行時序，測試沒觸發不代表不存在，必須從程式邏輯（是否有未受保護的共用資料寫入）分析，而不能只靠測試結果歸納。

---

# Interview Questions

- 為什麼 `counter++` 這種看似單一的操作會產生 race condition？
- 為什麼 race condition 特別難以透過重複測試發現？

延伸思考：如果把 `counter++` 換成 RISC-V 的組合語言（`lw` 讀取、`addi` 加一、`sw` 寫回），對照 `pipeline_hazard.skill` 描述的 RAW hazard，這裡的問題跟 pipeline hazard 有什麼本質上的不同？（提示：想想「誰在觀察相依關係」——是同一個核心的硬體，還是兩個獨立執行的核心）

---

# Summary

多執行緒共用同一個位址空間，能提升平行度但引入 race condition：看似單一的操作（例如 `counter++`）實際上由多個非原子步驟組成，多執行緒交錯執行這些步驟可能導致結果依賴不可預測的時序；race condition 不保證每次都重現，必須靠分析程式邏輯（而非測試結果）判斷是否存在，這是 `synchronization` 機制存在的直接理由。
