# Metadata

Name: locality
Category: computer_architecture/cache
Tags: [locality, temporal-locality, spatial-locality, cache-theory]
Dependencies: []
Related Skills: [memory_hierarchy, cache]
Difficulty: beginner

---

# Purpose

如果程式存取記憶體的模式是完全隨機、毫無規律，那麼不管設計多聰明的 cache 都無法提升效能——cache 之所以有效，是因為真實程式的記憶體存取模式**不是**隨機的，而是高度符合「區域性原理（Principle of Locality）」。這個 skill 是理解一切 cache 設計動機的第一步。

---

# Concepts

- **時間區域性（Temporal Locality）**：最近被存取過的資料，很可能在不久之後**再次**被存取。例如迴圈變數、迴圈內重複使用的資料。
- **空間區域性（Spatial Locality）**：一個位址被存取後，跟它**位址相鄰**的資料很可能在不久之後也被存取。例如陣列的循序存取、指令本身依序執行（指令記憶體的存取幾乎永遠符合空間區域性）。

---

# Theory

## 為什麼真實程式天生符合區域性

這不是巧合，而是程式設計的自然結果：迴圈（loop）反覆執行同一段指令、反覆存取同一組變數，直接對應時間區域性；陣列、結構體等資料結構在記憶體中連續排列，程式循序處理這些資料時直接對應空間區域性；函式呼叫時集中存取自己的 stack frame（見 `foundations/c_memory_layout`），也是空間區域性的體現。可以說，區域性幾乎是「結構化程式設計」的必然副產物，而不是需要程式設計者刻意去迎合的東西——但反過來說，如果程式設計者刻意寫出不符合區域性的存取模式（例如大跨步、隨機跳躍地存取巨大陣列），也會讓 cache 的效果大打折扣，這是效能工程中真實會發生的問題（見 `cache` skill 的 Common Mistakes）。

## 區域性原理是量化依據，不是「感覺」

`shared/jserv_style.skill` 要求量化勝過直覺——區域性原理不是「憑感覺」的假設，而是可以透過實際量測程式的記憶體存取軌跡（memory trace）、統計「距離上次存取同一個位址的間隔」（衡量時間區域性）、「距離上次存取的位址有多近」（衡量空間區域性）驗證的具體現象，這也是電腦架構研究中設計新 cache 策略時常見的量測方法。

---

# Hardware

（不適用 — 區域性是程式行為的統計特性，不是硬體本身的行為；`memory_hierarchy`、`cache` 才是利用這個特性的具體硬體設計）

---

# Software

```c
// 強烈符合時間 + 空間區域性
for (int i = 0; i < N; i++) {
    sum += array[i];   // array[i] 循序存取 -> 空間區域性
                        // sum、i 反覆被存取 -> 時間區域性
}

// 刻意破壞空間區域性（大跨步存取，僅供對照示範）
for (int i = 0; i < N; i += 1024) {
    sum += array[i];   // 每次跳很遠，同一個 cache block 幾乎不會被重複用到
}
```

---

# Example

矩陣運算是區域性原理最經典的教學案例：以 row-major（列優先）方式儲存的二維陣列，若用「先跑列、再跑欄」（`for row: for col: A[row][col]`）的順序存取，符合空間區域性（同一列在記憶體中連續）；若順序顛倒（`for col: for row: A[row][col]`），每次存取都跳到很遠的位址，破壞空間區域性，即使做的運算完全一樣，實際執行時間可能因為 cache miss 大增而顯著變慢。

---

# Common Mistakes

- 如果你認為「只要程式邏輯正確，執行順序不影響效能」，這個直覺假設了記憶體存取的成本是均一的，但在有 cache 的系統中，同樣的運算量、僅僅因為存取順序不同（是否符合區域性），實際執行時間可能差好幾倍——上方 Example 的矩陣存取順序問題可以透過實際量測（記錄兩種存取順序的執行時間）驗證。
- 把時間區域性跟空間區域性當成互相獨立、只會各自單獨出現的現象——實務上大多數真實程式同時符合兩者（見 Software 段的迴圈範例），cache 設計（例如一次搬移一整個 cache block/line，同時利用兩種區域性）也是同時針對兩者最佳化。

---

# Interview Questions

- 為什麼陣列的循序存取符合空間區域性，而鏈結串列（linked list）的隨機記憶體配置通常不符合？
- 怎麼用實際量測（而非憑印象）驗證一段程式碼是否符合區域性？

延伸思考：如果一個程式完全不符合時間區域性（每個變數只被存取一次，用完即丟），但高度符合空間區域性（永遠循序存取），cache 設計還能提供效能提升嗎？為什麼？

---

# Summary

區域性原理分時間區域性（最近用過的還會再用）與空間區域性（相鄰位址會被一起用），是真實程式（迴圈、陣列、stack frame 存取）的自然結果，也是 cache 之所以有效的理論根據；區域性程度可透過實際量測記憶體存取軌跡驗證，不是憑感覺的假設，違反區域性的存取模式會讓 cache 效果大打折扣。
