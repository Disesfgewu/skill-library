# Metadata

Name: instruction_formats
Category: computer_architecture/isa
Tags: [risc-v, instruction-encoding, r-type, i-type, immediate]
Dependencies: [risc_v_instructions]
Related Skills: [risc_vs_cisc, single_cycle_datapath]
Difficulty: intermediate

---

# Purpose

`risc_v_instructions` 說明指令做什麼，這個 skill 說明指令**在記憶體裡實際長什麼樣子**（32-bit 的 0/1 排列）。不懂編碼格式，就無法理解 `control_unit` 怎麼從一串 0/1 判斷出這是哪種指令、也無法理解為什麼 RISC-V 的立即值編碼會設計得那麼「不直覺」。

---

# Concepts

RISC-V 基本指令格式（皆固定 32-bit）：

- **R-type**（register）：`funct7 | rs2 | rs1 | funct3 | rd | opcode`，用於暫存器對暫存器運算（例如 `add`）。
- **I-type**（immediate）：`imm[11:0] | rs1 | funct3 | rd | opcode`，用於帶立即值的運算與 load（例如 `addi`、`lw`）。
- **S-type**（store）：`imm[11:5] | rs2 | rs1 | funct3 | imm[4:0] | opcode`，用於 store。
- **B-type**（branch）：類似 S-type 但立即值代表位址偏移，且以 2 的倍數編碼（最低位元恆為 0，不需儲存）。
- **U-type**（upper immediate）：`imm[31:12] | rd | opcode`，用於 `lui`、`auipc`。
- **J-type**（jump）：`imm[20|10:1|11|19:12] | rd | opcode`，用於 `jal`。

---

# Theory

## 為什麼 opcode、rd、rs1 在不同格式中位置固定

R/I/S/B 四種格式把 `opcode`（最低 7 bits）、`rd`（第 7-11 bits，若該格式有用到）、`rs1`（第 15-19 bits，若有用到）刻意排在**相同的位元位置**，即使某個格式其實不需要 `rd`（例如 S-type、B-type 沒有目的暫存器）。這個設計讓 Instruction Decode 的硬體可以在還沒完全判斷是哪種格式之前，就先把 `rs1`、`rs2` 的欄位接去 Register File 的讀取埠——省下一級判斷後才能開始讀暫存器的延遲，是直接服務硬體實作效率的編碼決策，而非隨意排列。

## 為什麼立即值的位元被拆散、順序打亂（尤其 S-type、B-type、J-type）

這是 RISC-V 編碼中最常讓人困惑、也最常被誤解的地方：S-type 把 12-bit 立即值拆成 `imm[11:5]` 跟 `imm[4:0]` 兩段、分別放在指令的頭尾；B-type、J-type 更誇張，立即值的位元順序被打散重排。原因同樣是為了讓 `rs1`、`rs2`、`opcode`、`funct3` 這些**跨格式共用**的欄位盡量落在相同位元位置（見上一段），立即值只好被迫「見縫插針」塞進剩下的空位，而不是要求它們用連續、直觀的順序——這是「解碼硬體簡單」與「立即值編碼直觀」兩個目標互相衝突下，RISC-V 選擇犧牲後者的具體結果。

## Sign Extension（符號延伸）

所有立即值都以二補數（`foundations/number_representation`）編碼並延伸成 32-bit：把立即值的最高位元（符號位元）複製填滿左邊所有更高位元。例如 I-type 的 12-bit 立即值 `111111111011`（-5 的 12-bit 二補數）延伸成 32-bit 會變成 `11111111111111111111111111111011`，數值仍是 -5，這是二補數表示法能直接支援「延伸位元數但數值不變」的特性。

---

# Hardware

I-type 格式（32 bits，由高位到低位）：
```
| imm[11:0] (12 bits) | rs1 (5 bits) | funct3 (3 bits) | rd (5 bits) | opcode (7 bits) |
```

R-type 格式：
```
| funct7 (7 bits) | rs2 (5 bits) | rs1 (5 bits) | funct3 (3 bits) | rd (5 bits) | opcode (7 bits) |
```

比較兩者：`opcode`（位元 0-6）、`rd`（位元 7-11，I-type 才有意義）、`funct3`（位元 12-14）、`rs1`（位元 15-19）在兩種格式中位置完全相同，這正是 Theory 段解釋的設計巧思。

---

# Software

判斷一條 32-bit 指令屬於哪種格式，第一步永遠是看最低 7 bits 的 `opcode`——這是所有格式共通、位置固定的欄位，`control_unit`（見 `datapath` sub-domain）正是用這 7 bits（必要時搭配 `funct3`/`funct7`）當查表輸入。

---

# Example

解碼 `add x1, x2, x3` 對應的 32-bit 指令：opcode = `0110011`（R-type 通用 opcode）、rd = `00001`（x1）、funct3 = `000`、rs1 = `00010`（x2）、rs2 = `00011`（x3）、funct7 = `0000000`（區分 add 而非 sub，sub 的 funct7 為 `0100000`）。`funct3` 相同（都是 `000`）但 `funct7` 不同，正是硬體用來區分 `add` 跟 `sub` 這兩個共用同一個 opcode 的指令的方式。

---

# Common Mistakes

- 如果你認為「立即值的位元應該照數值順序、從低位到高位排列在指令裡」，這個直覺很合理（因為那樣讀起來最直觀），但 S/B/J-type 的立即值刻意打散重排，原因是要讓其他共用欄位（rs1、rs2、opcode）位置對齊——直接用 [Specification: RISC-V ISA Manual] 對照 S-type 的位元圖，會發現 `imm[11:5]` 跟 `imm[4:0]` 確實被拆到指令兩端，不是文件寫錯。
- 忘記做 sign extension，直接把 12-bit 立即值當成無號數處理，導致負的立即值（例如 `addi x1, x2, -1`）被誤算成一個很大的正數。

---

# Interview Questions

- 為什麼 R-type 跟 I-type 指令的 `opcode`、`rd`、`rs1` 欄位刻意排在相同位元位置？
- `add` 跟 `sub` 的 opcode 與 funct3 都相同，硬體怎麼區分兩者？

延伸思考：如果要新增一個格式（例如某個 RISC-V extension 定義的新指令格式），設計者會優先考慮哪些既有欄位的位置不能動？為什麼？

---

# Summary

RISC-V 用 R/I/S/B/U/J 六種固定 32-bit 格式編碼指令，`opcode`/`rd`/`rs1`/`funct3` 等跨格式共用欄位位置對齊以簡化解碼硬體，立即值因此被迫拆散重排（尤其 S/B/J-type），且一律以二補數 sign extension 到 32-bit；判斷格式與功能的第一步永遠從 `opcode`（必要時搭配 funct3/funct7）查起。
