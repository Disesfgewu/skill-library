# Metadata

Name: risc_vs_cisc
Category: computer_architecture/isa
Tags: [risc, cisc, isa-design, x86, risc-v]
Dependencies: [risc_v_instructions]
Related Skills: [instruction_formats]
Difficulty: intermediate

---

# Purpose

理解 RISC-V 為什麼是現在這個樣子（load-store architecture、固定 32-bit 指令長度），需要對照它明確拒絕了什麼設計選項——也就是 CISC（Complex Instruction Set Computer）的哲學。這不是單純的「哪個比較好」的口水戰，而是一組具體、可驗證的設計取捨。

---

# Concepts

| 面向 | RISC（例：RISC-V） | CISC（例：x86） |
| --- | --- | --- |
| 指令長度 | 固定（RISC-V 基本指令固定 32-bit） | 可變（x86 指令 1 到 15 bytes 不等） |
| 記憶體存取 | 僅 load/store 可碰記憶體 | 多數指令可直接以記憶體位址為運算元 |
| 指令數量/複雜度 | 少量簡單指令 | 大量指令，含高階複雜操作（例如字串複製、迴圈計數的單一指令） |
| 定址模式 | 少數幾種 | 多種複雜定址模式 |
| 執行方式 | 通常一個指令對應少數幾個簡單步驟，直接對應硬體 | 複雜指令內部常被拆解成多個微指令（micro-ops）執行 |

---

# Theory

## 這組取捨的根源：編譯器時代 vs. 組合語言手寫時代

CISC 的設計哲學源自 1970 年代——當時記憶體昂貴、編譯器技術不成熟，程式常由人手寫組合語言，「一條指令做很多事」被視為優點：省記憶體（指令密度高）、省下人工寫更多行組合語言的力氣。RISC 的哲學則是 1980 年代之後、編譯器技術成熟後的反思：既然程式主要由編譯器產生，不需要遷就人工撰寫的方便性；與其硬體花大量電晶體支援複雜指令（但編譯器實際上很少產生這些複雜指令、或產生了也不一定比組合出來的簡單指令序列快），不如讓指令集簡單、規整，把電晶體預算花在其他地方（例如更多暫存器、更深的 pipeline、更大的 cache）。

## 指令長度固定對 pipeline 的直接影響

固定指令長度讓 `pipeline_overview` 的 IF 階段可以確定「下一個指令一定在 PC+4」（RISC-V 基本指令固定 4 bytes），不需要先解碼才知道這條指令多長、下一條指令從哪裡開始。可變長度指令集（x86）在這方面天生更複雜：IF 階段必須先部分解碼才能確定這條指令的長度，進而確定下一條指令的起始位置，這是 x86 系列處理器內部設計遠比教學用 RISC-V pipeline 複雜的直接原因之一。

## RISC 是否真的等於「指令數量少」

這是常見的簡化理解，需要更精確：現代 RISC-V 透過 extension（M：乘除法、A：atomic、F/D：浮點、C：壓縮指令等）疊加後，總指令數量並不算「少」。RISC 更精確的定義是設計哲學層次的——「指令**複雜度**低、每條指令的硬體實作直接、規整的編碼格式」，而不是單純數指令條數。

---

# Hardware

（見 `instruction_formats` 的固定格式圖；CISC 可變長度指令集的解碼硬體需要額外的「先判斷長度」邏輯，此處不展開，屬於超出本知識庫目前範圍的 x86 微架構細節）

---

# Software

同一段運算「把陣列元素加 1」（見 `risc_v_instructions` 的範例），CISC 風格（概念示意，非真實 x86 語法）可能允許：
```
INC [array + offset]      ; 一條指令：讀取、加1、寫回，全部在硬體內部完成
```
而 RISC-V 必須拆成三條指令（`lw` → `addi` → `sw`，見 `risc_v_instructions` 範例）。CISC 版本指令數量少（程式碼更精簡），但這條指令內部到底做了幾個「步驟」、每個步驟耗費多少週期，對寫程式的人是不透明的；RISC-V 版本每條指令做的事情都很直接、對應到 datapath 中明確的硬體動作，執行時間相對容易預測。

---

# Example

比較「呼叫函式並傳遞參數」：CISC 可能提供一條指令同時完成「把多個暫存器壓入堆疊」；RISC-V 則是多條個別的 `sw` 指令加上調整 stack pointer 的 `addi`，由編譯器（依 calling convention，見 `assembly` sub-domain）產生這一串固定樣式的指令。RISC-V 版本每一步都是獨立、可預測、可以被 pipeline 重疊執行的簡單指令；CISC 版本則要靠處理器內部把那條複雜指令拆解執行。

---

# Common Mistakes

- 如果你認為「RISC 指令集的指令數量一定比較少」，這個直覺來自 RISC 名稱本身的字面意思（Reduced Instruction Set），但現代 RISC-V 加上各種 extension 後，實際可用指令數量不見得比精簡過的 CISC 子集少——RISC 真正的核心是**指令複雜度低、格式規整**，不是單純的指令條數，可以對照 RISC-V ISA Specification 裡各 extension 疊加後的完整指令表驗證這一點。
- 把「RISC vs CISC」講成非黑即白的二元對立、其中一個絕對優於另一個——現代高階 x86 處理器內部其實也大量借用 RISC 概念（把複雜的 x86 指令在硬體內部拆解成類似 RISC 的微指令 micro-ops 後才真正執行），兩種哲學在現代處理器設計中早已互相借鑑，不是純粹二選一。

---

# Interview Questions

- 為什麼固定長度指令集能簡化 pipeline 的 IF 階段設計？
- 「RISC 指令數量比較少」這個說法在什麼意義下是對的、什麼意義下是不精確的？

延伸思考：如果要在一個記憶體極度受限的嵌入式系統上設計指令集，CISC 的「指令密度高」優勢是否還成立？RISC-V 的 C（壓縮指令）extension 想解決的是什麼問題？

---

# Summary

RISC 與 CISC 是源自不同時代背景（人工組合語言 vs. 編譯器時代）的指令集設計哲學：CISC 用少量複雜指令換取指令密度、RISC 用大量簡單、格式規整的指令換取硬體實作簡單與可預測的執行時間；固定指令長度直接簡化 pipeline 的 IF 階段設計；現代處理器設計中兩種哲學已大量互相借鑑，不宜視為絕對二元對立。
