# Metadata

Name: floating_point
Category: computer_architecture/foundations
Tags: [floating-point, ieee-754, precision]
Dependencies: [number_representation]
Related Skills: [number_representation]
Difficulty: intermediate

---

# Purpose

整數表示法（`number_representation`）只能表示整數，範圍也固定。但科學計算、圖形、機器學習都需要表示很大、很小、或帶小數的數值，且希望用固定的 bit 數涵蓋很大的動態範圍——這正是浮點數（floating point）要解決的問題：用「類似科學記號」的方式，犧牲一部分精度換取範圍。

---

# Concepts

**IEEE 754** 是目前幾乎所有處理器共同採用的浮點數標準。單精度（32-bit，`float`）格式：

```
| Sign (1 bit) | Exponent (8 bits) | Mantissa/Fraction (23 bits) |
```

- **Sign**：0 為正、1 為負。
- **Exponent**：用 **biased（偏移）表示法**，實際指數 = 儲存值 - 127（單精度的 bias 是 127）。
- **Mantissa（尾數）**：正規化（normalized）表示下，數值永遠是 `1.xxxxx × 2^exponent`，那個固定的前導 1 不需要儲存（implicit leading 1），只儲存小數點後的部分。

雙精度（64-bit，`double`）格式：1 bit 符號 + 11 bit 指數（bias 1023）+ 52 bit 尾數，原理相同、精度與範圍更大。

---

# Theory

## 為什麼用 biased exponent 而不是二補數表示指數

用 biased 表示法而非二補數的理由：讓浮點數在「當成純粹的無號整數比較」時，大小順序跟真正的數值大小順序一致（前提是符號相同）——這讓硬體可以用整數比較電路加速某些浮點數比較，是設計上的巧思。若用二補數表示指數，负指数（很小的數）在整數層面反而看起來「很大」，順序會被打亂。

## 為什麼有 implicit leading 1

正規化後的二進位科學記號，除了 0 本身，最高位一定是 1（因為只要不是 0，永遠可以调整指數讓最高有效位落在小數點前一位、且該位恆為 1）。既然這個 1 永遠存在、永遠固定,就不需要真的花一個 bit 儲存它，讓 23 個 bit 全部拿來儲存更精確的小數部分，多換到將近 1 個 bit 的有效精度。

## 精度問題的根源

浮點數的本質是「用有限的尾數 bit 數，逼近一個實數」，很多十進位小數（例如 0.1）換成二進位是無窮循環小數，尾數只有固定長度，必然被截斷/捨入，這就是為什麼 `0.1 + 0.2 != 0.3`（在浮點數運算下）的根本原因——不是硬體出錯，是表示法本身的必然限制。

---

# Hardware

以 -6.5 的 IEEE 754 單精度表示為例：
```
1. 符號: 負數 -> Sign = 1
2. 6.5 的二進位: 110.1
3. 正規化: 1.101 x 2^2
4. Exponent 實際值 = 2, 儲存值 = 2 + 127 = 129 = 10000001 (8 bits)
5. Mantissa: 正規化後小數點後部分 "101"，補滿 23 bits: 10100000000000000000000

最終 32-bit 表示:
1 10000001 10100000000000000000000
```

---

# Software

C/C++ 中 `float` 對應單精度、`double` 對應雙精度；兩個浮點數比較相等時通常不直接用 `==`（因為捨入誤差），而是判斷差值是否小於一個容許誤差（epsilon）——這正是本 skill 的 Theory 段解釋的「精度問題根源」在程式設計上的直接後果。

---

# Example

見上方 Hardware 段 -6.5 的完整編碼範例。反過來，若給定 `0 10000000 10000000000000000000000`：Sign=0（正）、Exponent 儲存值 128 → 實際指數 = 128 - 127 = 1、Mantissa = 1.1（含 implicit 1）→ 數值 = 1.1(二進位) × 2^1 = 11.0(二進位) = 3。

---

# Common Mistakes

- 忘記 implicit leading 1，把儲存的 23-bit mantissa 直接當成完整尾數而漏掉前面隱含的 `1.`。
- 忘記 exponent 是 biased 表示，直接把儲存的 8-bit 值當成實際指數，沒有減去 bias（127）。
- 誤以為浮點數精度問題是「硬體 bug」，而不理解是有限 bit 數必然導致的表示限制。

---

# Interview Questions

- 為什麼 IEEE 754 用 biased exponent 而不是二補數表示指數？
- 為什麼 `0.1 + 0.2 == 0.3` 在浮點數運算下通常是 false？

---

# Summary

IEEE 754 用 Sign + Biased Exponent + Mantissa（含 implicit leading 1）表示浮點數，用有限精度換取極大的動態範圍；biased exponent 讓整數比較電路可直接用於浮點數比較，implicit leading 1 多換一個 bit 精度；精度誤差是表示法本身的必然限制，不是硬體錯誤。
