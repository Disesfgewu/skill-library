# Metadata

Name: number_representation
Category: computer_architecture/foundations
Tags: [binary, twos-complement, number-representation, risc-v]
Dependencies: []
Related Skills: [floating_point, c_memory_layout]
Difficulty: beginner

---

# Purpose

硬體只認得高低電位（0 跟 1），但程式要處理正數、負數、字元、指令——所有這些都必須先約定好「一串 0/1 該怎麼解讀」，這個約定就是數字表示法。不先講清楚這件事，後面談 ALU 怎麼做減法、立即值怎麼編碼、溢位怎麼判斷都無從解釋起。

---

# Concepts

- **無號數（unsigned）**：N 個 bit 直接當成二進位數字，範圍 0 到 2^N - 1。
- **有號數（signed）— Two's Complement（二補數）**：目前幾乎所有處理器（含 RISC-V）採用的表示法。最高位元（MSB）代表符號，但不是簡單的「符號+絕對值」，而是：負數 x 的二補數表示 = 2^N - |x|（用 N-bit 運算）。
- **轉換技巧**：對一個數取二補數（正變負或負變正）＝「所有位元取反（invert）再加 1」。

---

# Theory

## 為什麼選二補數而不是「符號+絕對值」或「1's complement」

二補數的關鍵優勢：**加法/減法電路可以直接共用同一套硬體，不需要為正負號另外判斷**。用「符號+絕對值」表示時，加法器要先判斷兩數同號還是異號、再決定做加法還是減法，電路複雜很多；二補數則是不管正負，直接照無號數加法規則做加法，結果自動正確（多出來的進位直接捨棄）——這也是為什麼 CPU 的 ALU 只需要一個加法器就能做加法與減法（減法 = 加上被減數的二補數）。

`1's complement`（單純所有位元取反表示負數）有兩個缺點：有 +0 與 -0 兩種零的表示（浪費一個編碼、且比較邏輯要多處理一種情況），且加法需要額外的「end-around carry」修正，二補數兩者都沒有這個問題（只有一種 0 的表示，進位直接捨棄即為正確結果）。

## 範圍與溢位

N-bit 二補數表示範圍是 -2^(N-1) 到 2^(N-1) - 1（注意正負範圍不對稱：多一個負數）。溢位（overflow）判斷規則：**兩個同號數相加，結果卻變成異號，代表溢位**（例如兩個正數相加卻得到負數）。

---

# Hardware

以 4-bit 二補數為例，求 -5 的表示：
```
  5 的二進位：       0101
  所有位元取反：      1010
  加 1：             1011   <- 這就是 -5 的 4-bit 二補數表示
```

驗證：`1011` 當無號數是 11，二補數規則下代表 11 - 16 = -5，正確。

減法用加法實作（8 - 5，皆用 4-bit）：
```
  8  的二進位：       1000   (注意：4-bit 二補數表示範圍是 -8~7，這裡示範一般 N-bit 原理，
                              實務上 N 要夠大才不會讓 8 本身溢位；此處僅示範運算規則)
  5  的二補數(-5)：   1011
  相加：             10011   <- 5 個 bit，捨棄最高位進位，剩 0011 = 3，正確（8-5=3）
```

---

# Software

RISC-V 中，`addi` 指令的立即值（immediate）以二補數編碼，因此 `addi x1, x2, -5` 跟「用二補數表示的立即值直接做無號加法」是同一份硬體邏輯，這正是二補數在指令集設計上被廣泛採用的直接體現 [Specification: RISC-V ISA Manual]。

---

# Example

判斷 `1101`（4-bit）代表的有號數：MSB 是 1，代表負數。取二補數（反相加 1）：`1101` 反相 = `0010`，加 1 = `0011` = 3，所以原數是 -3。驗證：`1101` 當無號數是 13，二補數規則 13 - 16 = -3，一致。

---

# Common Mistakes

- 把二補數的負號判斷想成「跟正數一樣，只是最高位元代表『減去』這個絕對值」——正確理解是整個數字要重新解釋成 `2^N - |x|`，不是「符號位元 + 絕對值」那種簡單拼接。
- 溢位判斷只看「結果的符號位元」而忽略要跟「兩個運算元同號」這個條件一起看（例如一正一負相加不可能溢位，只有同號相加才可能）。

---

# Interview Questions

- 為什麼二補數只有一種 0 的表示法，而 1's complement 有兩種？這對硬體設計有什麼影響？
- 兩個 N-bit 二補數相加，怎麼用硬體邏輯判斷是否溢位？

---

# Summary

二補數是現代處理器（含 RISC-V）通用的有號數表示法，負數 x 表示為「取反加 1」，優勢是加法/減法可共用同一套加法器電路、且只有一種 0 的表示；溢位判斷規則是「同號相加卻得到異號結果」。
