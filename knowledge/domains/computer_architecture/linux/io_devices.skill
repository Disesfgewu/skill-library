# Metadata

Name: io_devices
Category: computer_architecture/linux
Tags: [io, memory-mapped-io, polling, device-registers]
Dependencies: []
Related Skills: [interrupts]
Difficulty: intermediate

---

# Purpose

前面所有 skill 談的都是處理器跟記憶體之間的互動,但處理器同樣需要跟鍵盤、硬碟、網路卡這些外部裝置溝通——這些裝置的速度通常比記憶體慢好幾個數量級（例如硬碟的存取時間以毫秒計，記憶體以奈秒計），這個巨大的速度落差直接決定了處理器該用什麼策略跟它們互動。

---

# Concepts

- **Memory-Mapped I/O（MMIO）**：把裝置的控制/狀態暫存器直接對應到處理器的虛擬位址空間中的某段位址，處理器用一般的 `lw`/`sw` 指令就能存取裝置，不需要專門的 I/O 指令。
- **Device Register**：裝置提供的暫存器，常見至少包含：**Status Register**（回報裝置目前狀態，例如「資料是否已就緒」）、**Data Register**（實際傳輸資料的暫存器）。
- **Polling（輪詢）**：處理器主動、重複地讀取裝置的 Status Register，檢查裝置是否已經準備好,若還沒準備好就繼續迴圈重複檢查。

---

# Theory

## 為什麼 Memory-Mapped I/O 比獨立的 I/O 指令更受青睞

如果用專門的 I/O 指令（某些架構確實有，例如 x86 的 `in`/`out`），處理器與編譯器就需要額外的一整套指令與定址方式專門處理裝置存取。MMIO 讓裝置存取重用**完全相同**的 `lw`/`sw` 指令與 `single_cycle_datapath`/`pipeline` 已經支援的資料路徑,不需要額外硬體,程式對裝置的存取在組合語言層次跟存取一般記憶體位址沒有語法上的差異——這正是 RISC-V 這類簡化指令集哲學（見 `isa/risc_vs_cisc`）的直接延伸：不為特殊用途另外設計指令，能用既有機制解決就不新增。

## Polling 的效能問題：忙碌等待浪費 CPU

Polling 最大的問題不是「做不到」，而是效率：處理器在裝置準備資料期間，必須**持續**執行檢查迴圈（不斷讀 Status Register），完全無法去做任何其他有意義的工作——如果裝置很慢（例如等待使用者按鍵，可能是幾秒鐘），這段期間處理器的運算能力被完全浪費。這個問題的量化嚴重程度,直接取決於「裝置速度」與「處理器速度」的落差——落差越大，polling 浪費的比例越高，這正是 `interrupts` 存在的直接理由。

---

# Hardware

MMIO 的位址空間配置示意：
```
虛擬位址空間:
+------------------+  高位址
|      Stack        |
|        |          |
|  (一般記憶體區域)   |
|        |          |
|      Heap         |
+------------------+
|   Data / Text     |
+------------------+
|  裝置暫存器（MMIO） |  <- 這段位址範圍不是真正的記憶體，
|  0xF0000000 起     |     讀寫這裡的位址會被路由到裝置控制器
+------------------+  低位址
```

---

# Software

Polling 讀取一個裝置的資料（概念示意，`STATUS_REG`、`DATA_REG` 是 MMIO 對應到的位址）：
```c
while ((*STATUS_REG & READY_BIT) == 0) {
    // 忙碌等待：不斷檢查裝置是否已就緒，這段期間 CPU 完全被這個迴圈佔用
}
int value = *DATA_REG;  // 裝置就緒後，讀取實際資料
```

---

# Example

假設處理器每秒可以執行 10 億次指令（1 GHz），但使用者平均每 500 毫秒才按一次鍵盤——如果用 polling 等待鍵盤輸入，處理器在這 500 毫秒（相當於 5 億個指令週期）內，可能什麼有意義的工作都沒做，只是不斷檢查「按鍵了嗎？按鍵了嗎？」，這 5 億個週期的運算能力被完全浪費。這個具體數字直接說明了為什麼「處理器速度遠快於裝置」這個常態下，polling 是效率極差的策略。

---

# Common Mistakes

- 如果你認為「MMIO 存取裝置暫存器，跟存取一般記憶體位址的效果完全一樣」，這個直覺忽略了裝置暫存器的讀寫**通常帶有副作用**——例如讀取某些裝置的 Data Register 可能會清空該暫存器（代表「已讀取」），或觸發裝置進行下一步動作，這跟讀取一般 RAM（讀取不會改變任何東西）行為完全不同；編譯器最佳化時也必須知道這一點（MMIO 位址通常要標記為 volatile，禁止編譯器把重複讀取最佳化成只讀一次），否則會產生跟裝置實際硬體行為不符的錯誤程式。
- 認為 polling 在任何情境下都是「差」的設計——如果裝置反應極快（例如反應時間短於一次 context switch 或中斷處理的開銷），polling 反而可能比中斷更有效率，這是效能取捨而非絕對的優劣判斷,需視裝置速度與處理器速度的實際落差量化評估。

---

# Interview Questions

- 為什麼 MMIO 讓裝置存取可以重用一般的 load/store 指令，不需要專門的 I/O 指令？
- Polling 讀取裝置暫存器，為什麼必須告訴編譯器該位址是 volatile？

延伸思考：如果一個裝置的反應時間跟中斷處理的開銷差不多，該用哪種策略？如果系統同時要監控 100 個裝置，polling 策略的成本會怎麼隨裝置數量成長？

---

# Summary

Memory-Mapped I/O 把裝置暫存器對應到位址空間，讓裝置存取重用一般的 load/store 指令；Polling 讓處理器主動、重複檢查裝置的 Status Register，實作簡單但在裝置遠慢於處理器時嚴重浪費運算能力（可具體量化）；裝置暫存器讀寫常帶有副作用，需標記 volatile 避免編譯器誤最佳化。
