# Metadata

Name: interrupts
Category: computer_architecture/linux
Tags: [interrupts, interrupt-driven-io, isr]
Dependencies: [io_devices]
Related Skills: [trap_handling]
Difficulty: intermediate

---

# Purpose

`io_devices` 說明 Polling 的核心問題：處理器必須主動、重複檢查裝置，浪費運算能力。中斷（Interrupt）反轉了這個關係——改成**裝置主動通知處理器**「我準備好了」，處理器在通知到達之前完全可以去做別的工作，這是解決 Polling 效率問題最直接的機制。

---

# Concepts

- **Interrupt（中斷）**：裝置透過一條專屬硬體訊號線，向處理器發出「需要注意」的請求，處理器目前執行的程式會被**非同步地**（不是程式自己請求的、可能在任何指令之間發生）暫停。
- **ISR（Interrupt Service Routine，中斷服務常式）**：中斷發生時，處理器跳去執行的一段特定程式碼，負責實際處理這次中斷（例如真正去讀裝置的資料）。
- **Interrupt Vector（中斷向量）**：把不同中斷來源（例如鍵盤、硬碟、計時器各自的中斷）對應到各自 ISR 位址的表格，讓處理器能依中斷來源跳到正確的處理程式碼。

---

# Theory

## 中斷跟一般函式呼叫的本質差異

`assembly/calling_convention` 描述的函式呼叫，是程式**主動**、**在已知的時間點**（執行到 `jal` 那一行）發起的；中斷則是**被動**發生、**時間點不可預測**（裝置什麼時候準備好，跟目前程式執行到哪一行完全無關）。這個差異帶來額外的複雜度：處理器被中斷時,正在執行的程式對此毫不知情、也沒有機會事先保存自己需要的暫存器（跟一般函式呼叫時呼叫者可以按照 calling convention 主動保存不同）——因此中斷發生時,硬體/作業系統必須**強制**保存足夠多的處理器狀態（見 `trap_handling`），確保中斷處理完後,能讓原本的程式完全無感知地繼續執行，就像什麼事都沒發生過一樣。

## Interrupt-Driven I/O 相對 Polling 的效能取捨

中斷解決了 Polling 忙碌等待的問題,但引入了新的成本：每次中斷都要付出「保存狀態 → 跳到 ISR → 執行 ISR → 恢復狀態」這整個流程的固定開銷（通常是幾十到幾百個週期）。如果一個裝置**非常頻繁**地產生中斷（例如高速網路卡每個封包都中斷一次），這個固定開銷乘上頻繁的次數，總成本可能反而超過 polling——這正是為什麼高效能網路裝置常常混合使用兩種策略（例如 NAPI：先用中斷通知「有資料來了」，之後短暫切換成 polling 快速處理一批資料，避免每個封包都付出一次完整的中斷開銷），是量化取捨而非「中斷永遠優於 polling」的簡單結論。

---

# Hardware

Interrupt Vector 查表流程示意：
```
中斷發生 --> 硬體讀取中斷來源編號 --> 查 Interrupt Vector Table
                                          |
                                          v
                              找到對應的 ISR 位址
                                          |
                                          v
                          PC 跳到該 ISR，開始執行中斷處理
```

---

# Software

用中斷改寫 `io_devices.skill` 的 polling 範例（概念示意）：
```c
// 主程式：發起讀取請求後，可以去做其他工作，不需要在這裡等待
start_device_read();
do_other_useful_work();   // CPU 沒有被浪費在忙碌等待上

// 中斷服務常式（裝置就緒時，硬體自動跳來這裡執行）
void isr_device_ready() {
    int value = *DATA_REG;   // 讀取實際資料
    notify_waiting_process(value);  // 通知原本在等資料的程式
}
```

---

# Example

比較「等待使用者按鍵」這件事，用 polling 跟用中斷的處理器使用率差異（延續 `io_devices.skill` 的例子：使用者平均每 500ms 按一次鍵，處理器 1GHz）：Polling 版本這 500ms 幾乎全部浪費在檢查迴圈；中斷版本這 500ms 期間，處理器可以完全去執行其他程式（例如處理其他使用者的請求），只有在按鍵真正發生的那個瞬間，才付出一次 ISR 的固定開銷（例如 100 個週期，相對 5 億個週期的閒置時間，比例微乎其微）——這正是中斷驅動 I/O 在裝置遠慢於處理器的常態下明顯優於 polling 的量化理由。

---

# Common Mistakes

- 如果你認為「中斷在任何情況下都比 polling 有效率」，`Theory` 段已經具體反駁：當裝置產生中斷的頻率極高、每次中斷的固定開銷佔比不可忽略時（例如高速網路卡逐封包中斷），中斷的總開銷可能反而超過改用短暫 polling 批次處理——實務上（如 Linux 的 NAPI 機制）常常混合兩種策略，不是非黑即白的二選一,必須依裝置的實際特性（速度、中斷頻率）量化評估。
- 把中斷發生的時間點想成「可預測、發生在特定指令邊界」——中斷是非同步事件，可能在處理器執行任何一條指令的過程中發生，硬體必須確保中斷處理不會破壞目前指令的正確執行（通常規定中斷只在指令完成後、下一條指令開始前才真正介入），這是 `trap_handling` 要處理的細節。

---

# Interview Questions

- 為什麼中斷驅動 I/O 不是在任何情境下都優於 polling？
- 中斷跟一般函式呼叫，在「處理器狀態如何被保存」這件事上有什麼本質差異？

延伸思考：如果一個系統同時有 10 個裝置都可能觸發中斷，且它們幾乎同時發生，硬體/作業系統該怎麼決定先處理哪一個？（提示：這是中斷優先權/中斷遮罩機制要解決的問題）

---

# Summary

中斷讓裝置主動通知處理器，處理器在通知到達前可以執行其他工作，解決 polling 忙碌等待浪費運算能力的問題；但每次中斷都有固定開銷（狀態保存、跳轉、恢復），中斷頻率極高時這個開銷可能反而超過 polling，是需要依裝置特性量化評估的取捨；中斷是非同步事件，跟主動發起的函式呼叫在狀態保存機制上有本質差異，這正是 `trap_handling` 要處理的問題。
